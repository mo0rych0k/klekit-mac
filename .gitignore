/target

# AI agent tooling — local only, never commit
.agents/
AGENTS.md
.cargo/
tasks.md
skills-lock.json
.antigravitycli/
.antigravity.cli/


# AI model weights — never commit (too large, stored on SSD separately)
/models/*.bin
/models/*.gguf
/models/*.pt
/models/*.ggml

# Root-level model weights
/*.bin
/*.gguf
/*.ggml
/*.pt

# Compiled binaries (auto-generated by cargo)
/bin/

# Cargo lock for libraries (keep for binaries)
# Cargo.lock  # intentionally tracked for binary crates

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Visual Brainstorming Companion
.superpowers/

# Documentation
docs/


